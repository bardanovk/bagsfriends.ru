<h2>Товары</h2>
<a href="/manage/products/create"><button>Создать товар</button></a>



{{#if products.length}}
{{!--
<ul>
    {{#each products}}
    <li>
        <label>
            <span>

                {{prodTitle}}
                {{category}}
                <div style="display:none" name="prodId">{{_id}}</div>
            </span> <a href="./products/edit/{{_id}}"><button type="button">Edit</button>
                {{!-- <form action="/manage/products/delete/{{_id}}" method="POST" style="display: inline-block;">
                    <button type="submit">delete</button>
                </form>
            </a>
        </label>
    </li>
    {{/each}}
</ul> --}}

<table class="manage-table">
    <tr class="manage-table-header">
        <th>Фото</th>
        <th>Наименование</th>
        <th>Категории</th>
        <th>Редактировать</th>
        <th>Видимость</th>
    </tr>
    {{#each products}}
    <tr>
        <th><img src="../image/products/{{prodTitle}}/Main.jpg" alt="картинка" width="50"></th>
        <th>{{prodTitle}}</th>
        <th>{{category}}</th>
        <th><a href="./products/edit/{{_id}}"><button type="button">Edit</button></th>
        <th>{{visible}}</th>
    </tr>

    {{/each}}
</table>
{{else}}
<h3>Товаров нет!</h3>
{{/if}}